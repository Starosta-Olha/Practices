const { getMethodBind  } = require('../src/getMethodBind');

describe('getMethodBind ', () => {

    test('',() => {
        expect(getMethodBind ()).toBe();
    });

    test('',() => {
        expect(getMethodBind ()).toBe();
    });

    test('',() => {
        expect(getMethodBind ()).toBe();
    });

    test('',() => {
        expect(getMethodBind ()).toBe();
    });

})